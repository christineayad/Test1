@model List<Test.EntityFramework.Models.ClientStatusVM>
<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3 ">
        <div class="row">
            <div class="col-12 text-center"><h4 class="text-white py-2"> Client List</h4></div>

        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">

                <a asp-controller="Client" asp-action="AddEdit" class="btn btn-primary">
                    <i class="bi bi-plus-circle-fill"></i> Create New Client
                </a>
            </div>
        </div>

        <table id="tblData" class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>First Name</th>

                    <th>Last Name</th>

                    <th>Date Birth</th>
                    
                    <th>Marital Status</th>

                    <th>Mobile</th>
                    <th>Email</th>
                  
                    <th>Operation</th>

                </tr>
            </thead>
         @*    <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td>@obj.FirstName</td>

                        <td>@obj.LastName</td>

                        <td>@obj.DateBirth</td>
                        <td>@obj.Status.Status</td>

                        <td>@obj.Mobile</td>
                        <td>@obj.Email</td>
                      
                        <td>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Client" asp-action="AddEdit" asp-route-id="@obj.Id" class="btn btn-primary mx-2">
                                    <i class="bi bi-pencil-square"></i> Edit

                                </a>

                                <a asp-controller="Client" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash-fill"></i> Delete



                                </a>
                            </div>
                        </td>

                    </tr>
                }

            </tbody> *@

        </table>
    </div>
</div>

@section Scripts{
    <script src="~/js/client.js"></script>
}
